{
  "name": "@otiai10/too",
  "version": "1.0.0",
  "description": "Combine multiple processes' Stdout/Stderr/SIGINT to keep them all foreground",
  "scripts": {
    "build": "tsc",
    "validate": "npm run build && npm link && too -c 'echo foo' -c 'echo bar'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "too": "dist/bin/too.js"
  },
  "repository": {
    "type": "git",
    "url": "github.com/otiai10/too.js"
  },
  "author": "otiai10",
  "license": "MIT",
  "keywords": [
    "Command",
    "Parallel",
    "SIGINT",
    "ctrl+c"
  ],
  "dependencies": {
    "lookpath": "0.0.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "typescript": "^3.4.5"
  }
}
